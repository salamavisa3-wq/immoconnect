{
  "name": "sakeurimmo-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Outillage de déploiement Cloudflare Workers pour SakeurImmo (voir backend/ pour l'app elle-même)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
